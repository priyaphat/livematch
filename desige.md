# LiveMatch POS — Design Source of Truth

เอกสารนี้เป็นข้อกำหนดหลักสำหรับการออกแบบและพัฒนา LiveMatch POS เพื่อให้ UI, UX และ API ของแต่ละหน้าไปในทิศทางเดียวกัน

อัปเดตล่าสุด: 22 สิงหาคม 2026

## 1. Product Direction

- POS เป็นระบบเสริมของ LiveMatch และใช้บัญชีผู้ดูแลชุดเดียวกับ LiveMatch Admin
- ข้อมูล POS ทุกชุดต้องแยกตาม `admin_id` ของผู้ที่ Login
- รองรับร้านหรือสนามหลักเพียงแห่งเดียวต่อ Admin ในเวอร์ชันนี้ ไม่มีระบบหลายสาขา
- เชื่อม API ทีละหน้า หน้าที่เชื่อมแล้วต้องไม่ย้อนกลับไปใช้ mock data หรือ localStorage เป็นแหล่งข้อมูลหลัก
- UI ภาษาไทยเป็นหลัก และเตรียมโครงสร้างให้รองรับภาษาอังกฤษในอนาคต

## 2. Visual Design

- รูปแบบเรียบ ทันสมัย อ่านง่าย เหมาะกับหน้าจอขายที่ต้องใช้งานรวดเร็ว
- Light mode และ Dark mode
- สีหลัก: แดงสำหรับ action สำคัญ, เหลืองสำหรับ highlight, เขียวสำหรับสถานะสำเร็จ, แดงอ่อนสำหรับแจ้งเตือน
- พื้นหลัง Light mode ใช้โทนกระดาษถนอมสายตา ไม่ใช้สีขาวจ้าทั้งหน้า
- ปุ่มหลักใช้ทรงมน ข้อความสั้น และมี icon เฉพาะเมื่อช่วยให้เข้าใจเร็วขึ้น
- ตารางต้องอ่านง่าย รองรับ horizontal scroll บนหน้าจอเล็ก
- Mobile-first แต่หน้าขายและตารางต้องใช้งานบน desktop ได้เต็มพื้นที่
- Typography บนหน้าจอ POS ใช้ขนาดสูงกว่า Tailwind default หนึ่งระดับทั้งระบบเพื่อให้อ่านง่าย โดยไม่เพิ่มขนาดตัวอักษรของใบเสร็จตอนพิมพ์

## 3. Authentication

- Owner ใช้ LiveMatch Admin email/password หรือ `Admin No.` กับรหัสผ่านเดิม
- Owner ใช้อีเมลหรือ `Admin No.` + รหัสผ่าน ส่วน Staff ใช้อีเมลหรือ `Staff Number` + PIN ตัวเลข 4-6 หลัก โดย PIN ต้องเก็บแบบ bcrypt hash เท่านั้น
- อีเมล Staff เป็นข้อมูลไม่บังคับ แต่เมื่อกรอกแล้วต้องไม่ซ้ำกับอีเมล Owner หรือ Staff คนใดในระบบ และใช้เข้าสู่ POS ได้โดยไม่สนตัวพิมพ์เล็กใหญ่
- ไม่มีระบบสมัครสมาชิกใน POS
- ไม่มีปุ่ม “ลืมรหัสผ่าน” ใน POS
- มี checkbox “จดจำการเข้าสู่ระบบ”
  - ติ๊ก: ใช้ persistent cookie
  - ไม่ติ๊ก: ใช้ browser-session cookie
- เมื่อเปิด POS ต้องเรียก `/api/auth/pos/me` เพื่อตรวจ session และสิทธิ์ POS
- การ Login จากหน้า POS ต้องใช้ `/api/auth/pos/login` และอนุญาตเฉพาะ Admin ที่ Backoffice เปิด `pos_enabled` แล้ว
- เมื่อ Backoffice ปิดสิทธิ์ POS ต้องเข้าไม่ได้ทั้งบัญชี Admin และ Staff email/Staff Number/รหัสในเครือทั้งหมด รวมถึง session เดิมและ POS API ทุก endpoint
- Logout ต้องเรียก `/api/auth/pos/logout` และ revoke session ฝั่ง backend
- ห้ามเก็บ password หรือ auth token ใน localStorage

## 4. Admin Identity

- Admin ทุกบัญชีมี `posAdminNumber` แบบถาวรและไม่ซ้ำ เริ่มจาก 1001
- Navbar แสดงชื่อ Admin และ `Admin No.`
- ไม่แสดงชื่อสาขา
- ไม่แสดงช่องค้นหาสินค้าด่วนบน Navbar
- ชื่อและเลข Admin ต้องมาจากข้อมูล Login API ไม่ใช่ mock settings
- สมาชิก POS รวม Owner สูงสุด 3 คนต่อ Admin
- Staff Number ใช้รูปแบบ `{Admin No.}-{ลำดับ 2 หลัก}` เช่น `1001-01`
- Staff session แยกจาก LiveMatch Admin session และใช้เข้า API นอกขอบเขต POS ไม่ได้

## 5. Sale API & Payment Rules

- UI หน้า Sale, ตะกร้า, ส่วนลด, พักยอด และ Payment modal เป็น UI lock ห้าม redesign; เปลี่ยนเฉพาะการเชื่อมข้อมูลและ validation
- ราคาขาย, ส่วนลด, VAT, ยอดชำระ, เงินรับ และเงินทอนใช้จำนวนเต็มหน่วยสตางค์เป็น source of truth โดย backend คำนวณใหม่จากสินค้าในฐานข้อมูลเสมอ
- สูตรเรียงเป็น subtotal → ส่วนลด → VAT; VAT แบบรวมแยกจากยอดหลังส่วนลด ส่วน VAT แบบแยกนอกบวกหลังส่วนลด และ VAT ปิดต้องไม่คำนวณหรือแสดงบนใบเสร็จ
- การพักยอดเลือกได้เฉพาะสมาชิก active ของ Admin เท่านั้น แต่ละการพักสร้างเอกสาร `open` แยกและตัด/จองสต็อกทันที; ยกเลิกต้องคืนสต็อกใน transaction เดียว
- ช่องเลือกสมาชิกใน modal พักยอดต้องใช้ Combobox UI เดิม ค้นหาชื่อหรือเบอร์โทรผ่าน API หลังหยุดพิมพ์ 500 ms และห้ามใช้ข้อความที่พิมพ์เองเป็นสมาชิก
- สมาชิกคนเดิมพักได้หลายเอกสาร เมื่อชำระให้รวมยอดค้าง Match และ POS ด้วย Payment เดียว แต่ประวัติ Match/POS แยกกันผ่าน allocation และอ้างอิง Payment ID เดียวกัน
- POS สืบทอด PromptPay จากระบบจองสนามเป็นค่าเริ่มต้นและ override เฉพาะ POS ได้ การแสดง QR ใช้ QR ตามยอดก่อน แล้ว fallback เป็นรูป QR ที่อัปโหลดไม่เกิน 2 MB
- PromptPay รอบนี้ให้ Staff กดยืนยันรับชำระเอง ไม่มีการตรวจสลิปหรือธนาคารอัตโนมัติ
- Sale, Payment และ Stock movement ต้องบันทึก actor ID/type/name ของ Owner, Manager หรือ Cashier และใช้ idempotency key ป้องกันการตัดสต็อกซ้ำ

## 6. Navigation

เมนูหลักปัจจุบัน:

1. แดชบอร์ด
2. หน้าการขาย
3. บิล & ประวัติ
4. จัดการสินค้า
5. จัดการสต็อก
6. รายงาน
7. ตั้งค่า

กฎ Navigation:

- Bottom navigation ต้องคงตำแหน่งและมองเห็นง่าย
- หน้าที่ active ใช้พื้นแดงและข้อความสีขาว
- Navbar มีปุ่มจอลูกค้า, Theme, พักยอด, เสียง, Fullscreen และ Logout
- ไม่มี Quick Search บน Navbar

## 7. Products & Catalog

หน้า “จัดการสินค้า & แคตตาล็อก” เชื่อม API แล้ว และเป็น source of truth สำหรับข้อมูลสินค้า

> UI LOCK: หน้านี้ต้องใช้ UI ต้นฉบับจาก `pos_c.zip` ห้าม redesign, เปลี่ยน layout, เปลี่ยนรูปแบบตาราง, เปลี่ยนสี หรือสร้าง component หน้าตาใหม่ การพัฒนาต่อให้แก้เฉพาะ data/API layer และซ่อนเฉพาะ feature ที่ระบุว่าไม่ใช้งานเท่านั้น

### 6.1 Sections

- รายการสินค้า
- ตารางหมวดหมู่
- ตารางหน่วยนับ

กฎ Modal:

- Modal จัดการหมวดหมู่และหน่วยนับต้องค้างอยู่หลังเพิ่มหรือแก้ไข เพื่อให้จัดการหลายรายการต่อเนื่องได้
- หลังบันทึกให้ล้างช่องกรอกและกลับสู่โหมดเพิ่มรายการใหม่
- ปิด Modal เมื่อผู้ใช้กดปุ่มปิดเท่านั้น
- Modal เพิ่มหรือแก้ไขสินค้าปิดหลังบันทึกสำเร็จได้ตามปกติ

กฎช่องตัวเลข:

- เมื่อผู้ใช้กดช่องราคา ต้นทุน จำนวน VAT ส่วนลด หรือยอดเงิน ให้เลือกค่าปัจจุบันทั้งหมด เพื่อพิมพ์ค่าใหม่แทน `0` ได้ทันที
- ห้ามเกิดเลขศูนย์นำหน้า เช่น `010` จากค่าเริ่มต้นของระบบ
- ช่องราคาสินค้า ต้นทุน สต็อก และจุดเตือนสต็อกต้องปล่อยว่างได้ระหว่างพิมพ์ และค่อยแปลงเป็นตัวเลขเมื่อบันทึก
- หากผู้ใช้พิมพ์เลขศูนย์นำหน้า เช่น `040` ให้ normalize และแสดงเป็น `40` ทันที
- ซ่อนลูกศรเพิ่ม/ลดของ input ชนิด number ทุกหน้า ผู้ใช้กรอกตัวเลขโดยตรง
- ตอนแก้ไขสินค้า ห้ามแก้ SKU และจำนวนสต็อกเริ่มต้น; การเปลี่ยนยอดสต็อกต้องทำผ่านเมนูจัดการสต็อกเพื่อเก็บประวัติ

### 6.2 Product List

- แสดงรูปและชื่อสินค้าใน column เดียวกัน
- ถ้าสินค้าไม่มีรูป ให้ใช้ `/images/product_default.png` เป็นรูป default ในทุกหน้า POS
- รูปสินค้าต้นฉบับต้องไม่เกิน 2 MB และ frontend ต้องย่อด้านยาวไม่เกิน 1280 px พร้อมบีบอัดเป็น WebP ก่อนส่ง API
- Backend ต้องตรวจชนิดและจำกัดข้อมูลรูปสินค้าไม่เกิน 2 MB
- แสดง SKU และ Barcode
- แสดงหมวดหมู่และหน่วยนับ
- แสดงราคาขาย ต้นทุน สต็อก สถานะ และปุ่มจัดการ
- ใช้ server-side pagination
- เลือกจำนวนต่อหน้าได้ 10, 20 หรือ 50 รายการ
- ค้นหาด้วยชื่อสินค้า, SKU หรือ Barcode
- กรองด้วยหมวดหมู่และสถานะวางขาย/ปิดขาย
- สินค้าสต็อกต่ำต้องมีสีและ icon แจ้งเตือน

### 6.3 Add/Edit Product

ข้อมูลสินค้า:

- ชื่อสินค้า
- SKU
- Barcode
- หมวดหมู่
- หน่วยนับ
- ราคาขาย
- ต้นทุน
- สต็อกเริ่มต้น เฉพาะตอนสร้างสินค้า
- ระดับแจ้งเตือนสต็อกต่ำ
- สถานะวางขาย/ปิดขาย
- รายละเอียดสินค้า
- รูปสินค้า JPG, PNG หรือ WebP ไม่เกิน 2 MB

กฎสำคัญ:

- สินค้าที่สร้างแล้วห้ามแก้ยอดสต็อกจาก modal สินค้า ให้แก้ผ่านหน้าจัดการสต็อกเพื่อรักษาประวัติ
- การลบสินค้าใช้ Soft Delete หรือ “ปิดขาย” เพื่อเก็บประวัติบิลและสต็อก
- ไม่มี “เลือกรูปสำเร็จรูปยอดนิยม”
- ไม่มี “ผูกตัวเลือกโน้ต & ตัวเลือกเสริมกับสินค้านี้”
- ไม่มีระบบจัดการตัวเลือกโน้ตและส่วนผสมในหน้าแคตตาล็อก

### 6.4 Categories and Units

- แสดงเป็นตาราง ไม่ใช่รายการ mock
- เพิ่ม แก้ไข เปิด/ปิด และลบผ่าน API
- แสดงจำนวนสินค้าที่ใช้งานรายการนั้น
- ห้ามลบหมวดหมู่หรือหน่วยนับที่มีสินค้าใช้งานอยู่
- เมื่อเปลี่ยนชื่อหมวดหมู่หรือหน่วยนับ backend ต้องปรับสินค้าเดิมของ Admin เดียวกัน

## 8. Stock Management

> UI LOCK: หน้า “จัดการคลังสินค้า & รายการสต็อกรวม” ต้องคง UI ต้นฉบับของ `StockView` ทั้ง layout, สี, การ์ด, ตาราง, tab และ modal ห้าม redesign การพัฒนาต่อแก้เฉพาะ data/API layer และเพิ่ม control ที่จำเป็นภายใน design system เดิมเท่านั้น

- การ์ด dashboard, เอกสารรวม, ประวัติ movement และ supplier ใช้ PostgreSQL ผ่าน API เป็น source of truth ห้ามกลับไปอ่าน Stock/Supplier mock จาก localStorage
- การรับเข้า จ่ายออก และปรับยอดต้องบันทึกเป็นประวัติการเคลื่อนไหว
- เอกสารรับเข้าต้องเก็บ `supplier_id` และชื่อ supplier แบบ snapshot เพื่อให้เอกสารย้อนหลังไม่เปลี่ยนตามชื่อปัจจุบัน
- ปุ่ม “เปิดใบรับสินค้าจากเจ้านี้” ต้องเปิด modal รับเข้าและเลือก supplier รายการนั้นให้อัตโนมัติ
- ส่วนลดรับเข้าสินค้ารองรับจำนวนเงินบาทและเปอร์เซ็นต์ไม่เกิน 100% โดยกรอกได้ 2 ตำแหน่ง
- Modal รับเข้าต้องคำนวณยอดก่อนลด, ส่วนลด และยอดสุทธิแบบ real-time ภายในพื้นที่สรุปเดิม
- กระจายส่วนลดเท่ากันตามจำนวนหน่วยสินค้ารวม เศษสตางค์กระจายตามลำดับรายการอย่างคงที่ และห้ามต้นทุนสุทธิรายการใดต่ำกว่าศูนย์
- ต้นทุนสินค้าใหม่ใช้ weighted average จากมูลค่าสต็อกเดิมบวกมูลค่ารับเข้าสุทธิหลังส่วนลด หารจำนวนคงเหลือหลังรับเข้า และปัดครึ่งขึ้นเป็นสตางค์
- จ่ายออกใช้ต้นทุนเฉลี่ยปัจจุบัน และ transaction ต้องยกเลิกทั้งเอกสารหากรายการใดทำให้สต็อกติดลบ
- Modal “บันทึกตรวจนับและปรับปรุงยอดสต็อกจริง” ต้องแสดง:
  - สต็อกก่อนปรับ
  - สต็อกหลังปรับ
  - ยอดเพิ่มหรือลด
  - มูลค่าก่อนปรับ
  - มูลค่าหลังปรับ
  - ผลต่างมูลค่า
- ใช้สีเขียวเมื่อยอดเพิ่ม สีแดงเมื่อยอดลด และสีเทาเมื่อไม่เปลี่ยน
- การปรับยอดต้องเก็บจำนวนและมูลค่าก่อน/หลัง พร้อมผลต่าง แม้ยอดที่ตรวจนับได้ไม่เปลี่ยน
- หน้าสต็อกใช้รายการสินค้า API ชุดเดียวกับหน้าแคตตาล็อก
- เอกสารเลขที่เดิมห้ามบันทึกซ้ำ

## 9. Settings

- ตั้งค่า VAT ต้องมี toggle เปิด/ปิด
- เมื่อปิด VAT:
  - ไม่คำนวณ VAT
  - ไม่แสดง VAT ในตะกร้า จอลูกค้า และใบเสร็จ
- ไม่มีเมนูสำรองข้อมูล
- ไม่มีเมนูรีเซ็ตข้อมูล
- มี tab สิทธิ์
- มี tab สมาชิก POS จำกัดสูงสุด 3 คน
- สมาชิก, PIN hash, session และสิทธิ์ตามบทบาทใช้ PostgreSQL เป็น source of truth ห้ามใช้ localStorage
- บทบาทมี Owner, Manager และ Cashier; Owner มีทุกสิทธิ์และเป็นผู้จัดการสมาชิก/PIN/สิทธิ์
- การเปลี่ยนบทบาท สิทธิ์ สถานะ หรือการปิด `pos_enabled` ต้องมีผลกับ request ถัดไปทันที

## 10. API and Data Rules

- API ใช้ prefix `/api/admin/pos/`
- ทุก endpoint ต้องตรวจ Owner Admin session หรือ POS Staff session และตรวจ `pos_enabled`
- ทุก mutation ต้องตรวจ permission ของเมนูฝั่ง server; การซ่อนเมนูใน UI อย่างเดียวไม่ถือว่าเพียงพอ
- ทุก query และ mutation ต้อง scope ด้วย `admin_id`
- Unsafe request ต้องส่ง CSRF token
- Error จาก API ต้องแสดงเป็นข้อความภาษาไทยที่เข้าใจง่าย
- เมื่อ API ตอบ 401 ให้กลับหน้า Login
- ห้ามให้หน้า API-connected fallback ไปใช้ mock data โดยไม่แจ้งผู้ใช้
- Product pagination, search และ filter ต้องทำที่ server
- หน้าการขายต้องแสดงชื่อหมวดหมู่สินค้า ห้ามแสดง category id/code ต่อผู้ใช้
- รายละเอียดชำระเงินฝั่ง Match ต้องอ่าน POS snapshot และแสดงชื่อสินค้า ห้ามแสดงเพียงเลข `sale-*`
- ต้นทุนสินค้า เอกสารรับเข้า movement และต้นทุนขายใช้จำนวนเต็มหน่วยสตางค์เป็น source of truth
- คอลัมน์ต้นทุนหน่วยบาทเดิมต้อง sync แบบปัดเศษเพื่อให้หน้าเดิมยังทำงานได้ระหว่าง migration
- รูปสินค้าปัจจุบันเก็บเป็น Base64 ใน PostgreSQL; production ระยะยาวควรย้ายไป object storage

## 11. PWA

- POS ติดตั้งจาก browser เป็น icon บนหน้าจอได้
- ต้องมี manifest, service worker และ icon ขนาดมาตรฐาน
- ปุ่มติดตั้งแสดงเฉพาะเมื่อ browser รองรับและยังไม่ได้ติดตั้ง
- PWA ต้องเปิดกลับมาที่ POS port/domain เดิม

## 12. Current API Migration Status

| Page | Status | Source of truth |
|---|---|---|
| Login | Connected | LiveMatch Auth API |
| Products & Catalog | Connected | PostgreSQL via POS API |
| Sales | Connected | PostgreSQL via POS Sale/Billing API |
| Bills & History | Connected | Central Billing Payment + allocation snapshots |
| Stock & Suppliers | Connected | PostgreSQL via POS Stock/Supplier API |
| Reports | Pending | Mock/localStorage |
| Settings | Pending integration | localStorage; backend settings API บางส่วนพร้อมแล้ว |
| POS Members & Permissions | Connected | PostgreSQL via POS Staff/Permission API |

## 13. Next Priorities

1. เชื่อมตั้งค่า VAT, ร้าน และใบเสร็จให้ครบทุกส่วน
2. เชื่อม Dashboard และ Reports จากข้อมูลขายจริง
3. พิจารณา Barcode scanner, Excel import/export และ object storage สำหรับรูปสินค้า

## 14. Do Not Reintroduce

- ระบบหลายสาขาในเวอร์ชันปัจจุบัน
- Quick Product Search บน Navbar
- ปุ่มลืมรหัสผ่านใน POS
- รูปสินค้าสำเร็จรูปยอดนิยม
- ตัวเลือกโน้ตและส่วนผสมในหน้าแคตตาล็อก
- การลบสินค้าที่มีประวัติแบบถาวร
- การแก้สต็อกสินค้าเดิมจาก modal เพิ่ม/แก้ไขสินค้า
- Backup และ Reset ในหน้า Settings
- Mock data ในหน้าที่ประกาศว่าเชื่อม API แล้ว

## 15. Central Billing: Match + POS

- สมาชิกที่ผูก `member_id` ใช้ `billing_account` กลางร่วมกันระหว่าง Match และ POS; ผู้เล่นขาจรไม่รวมข้ามระบบ
- เมื่อเพิ่มสมาชิกหลักเข้า Session ต้องสร้างหรือผูก billing account ทันที เพื่อให้ POS เห็นยอด Match แม้ยังไม่มีรายการสินค้า
- หน้า POS แสดงพักยอดเพียงหนึ่งการ์ดต่อสมาชิก แต่เอกสาร Match และ POS แต่ละรายการยังแยกเก็บเพื่อ audit
- สมาชิกที่มียอด Match ค้างต้องแสดงในการ์ดพักยอดของ POS แม้ยอด POS เป็นศูนย์และไม่เคยมี Sale เพื่อให้รับชำระที่ POS ได้
- ยอด Match คำนวณสดจาก Session, ค่าเข้าสนาม และลูกแบดล่าสุดจนถึง Transaction ที่ชำระ
- การชำระต้องปิดยอดค้างทั้งหมดของสมาชิกในครั้งเดียว ไม่รองรับแบ่งจ่ายหรือเลือกเฉพาะ source
- Match และ POS ใช้ settlement service เดียวกัน โดย lock billing account และ source records แล้วคำนวณยอดใหม่ก่อนตัดเงิน
- Browser ต้องส่งยอดรวมที่เห็นล่าสุด; หากยอดเปลี่ยน backend ตอบ `409` พร้อมยอดใหม่และห้ามบันทึกจนกว่าผู้ใช้จะยืนยันอีกครั้ง
- Payment กลางหนึ่งรายการเก็บ `origin_system` (`match` หรือ `pos`) ผู้รับเงิน วิธีชำระ เงินรับ เงินทอน และ reference
- Allocation ทุก source ต้องเก็บ snapshot ณ เวลาชำระ:
  - Match: Session, สมาชิก, ค่าสนาม, ค่าลูกแบด และรายละเอียดที่ใช้คำนวณ
  - POS: เลข Sale, สินค้า, จำนวน, ราคาต่อหน่วย, ส่วนลด, VAT และยอดสุทธิ
- Payment กลางที่สำเร็จแล้วห้ามยกเลิกหรือเปิดยอดคืนจาก Match/POS; POS ยังคงยกเลิก Sale ที่พักและยังไม่ชำระได้พร้อมคืนสต็อกใน Transaction เดียว
- ประวัติ Match แสดง Payment กลางของสมาชิกใน Session ปัจจุบัน ส่วน POS แสดง Payment กลางทุก Session ภายใต้ Admin เดียวกัน
- ประวัติทั้งสองฝั่งต้องแสดงแหล่งรับชำระ ยอด Match/POS/รวม ผู้ทำรายการ วิธีชำระ และรายละเอียด allocation
- ทั้งสองระบบ refresh เฉพาะข้อมูล Billing ทุก 10 วินาที, refresh ทันทีเมื่อกลับมาเปิดแท็บและหลังทำรายการ และหยุด polling เมื่อแท็บถูกซ่อนหรือออกจากระบบ
- Billing sync ห้ามเขียนทับสถานะเกม คิว สนาม หรือข้อมูล Match ที่อีกเครื่องกำลังแก้ไข
- UI เดิมเป็นข้อบังคับ เพิ่มได้เฉพาะยอดแยก badge แหล่งที่มา และ modal รายละเอียดภายใน design system เดิม
