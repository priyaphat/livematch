# LiveMatch POS — Design System & UX Guardrails

> เอกสารนี้คือ source of truth ด้าน UX/UI สำหรับหน้า POS ของ LiveMatch  
> ใช้ประกอบการออกแบบ พัฒนา และ review ทุกครั้งที่แก้ `frontend/src/pages/POSPage.vue`

## 1. Product direction

POS นี้ออกแบบสำหรับผู้ดูแลสนามแบดที่ต้องขายสินค้าและเก็บเงินระหว่างดูแลสนามไปพร้อมกัน จึงต้อง **เร็ว อ่านง่าย กดง่าย และผิดพลาดยาก** โดยเฉพาะบนมือถือ

บุคลิกของงาน:

- เรียบ กระชับ และดูเป็นงานกีฬา
- ใช้พื้นโทนกระดาษถนอมสายตา ไม่ขาวจ้า
- สีเขียวสนามแบดเป็นสีหลัก
- ข้อมูลสำคัญ เช่น ยอดเงิน สต็อก และสถานะ ต้องมองเห็นได้ทันที
- รองรับ Light mode และ Dark mode เท่าเทียมกัน
- โครงสร้างรองรับ `th/en` แต่ภาษาไทยเป็นภาษาหลักของ v1

หลักตัดสินใจเมื่อพื้นที่ไม่พอ: **ความถูกต้องของยอด > ความเร็วในการขาย > ความสวยงาม > ข้อมูลรอง**

## 2. ผู้ใช้และบริบท

ผู้ใช้หลักคือ admin หรือพนักงานสนาม ซึ่งมักใช้งานในสภาพต่อไปนี้:

- ใช้โทรศัพท์มือเดียวหรือแท็บเล็ตบนเคาน์เตอร์
- ต้องสลับระหว่างขายสินค้า ดูบิล และดูแลเกม
- มีแสงในสนามไม่สม่ำเสมอ
- ต้องค้นหาสินค้าหรือชื่อลูกค้าอย่างรวดเร็ว
- การแตะผิดอาจทำให้ยอดเงินหรือสต็อกคลาดเคลื่อน

ทุก flow หลักควรจบได้ด้วยการแตะน้อยที่สุด และต้องไม่พึ่ง hover เพื่อให้ใช้งานได้

## 3. Information architecture

เมนูหลักของ POS ใช้ bottom navigation ตามลำดับนี้เสมอ:

1. แดชบอร์ด
2. การขาย
3. บิล
4. สินค้า
5. สต็อก
6. รายงาน
7. ตั้งค่า

กติกา:

- ห้ามเปลี่ยนชื่อหรือลำดับเมนูโดยไม่มีเหตุผลด้าน product
- แสดงไอคอนคู่กับข้อความเสมอ ห้ามใช้ไอคอนอย่างเดียว
- เมนูที่ active ใช้พื้น `court-500` และข้อความสีขาว
- Bottom navigation ต้องคงที่ด้านล่าง รองรับ safe area และเลื่อนแนวนอนได้เมื่อพื้นที่ไม่พอ
- เนื้อหาหลักต้องมี bottom padding มากพอ ไม่ถูก navigation บัง
- ถ้า POS ยังไม่เปิดใช้งาน ซ่อนเฉพาะ flow ที่ทำรายการขาย แต่ยังดูข้อมูลและตั้งค่าได้ตามสิทธิ์

## 4. Layout system

### Breakpoints

- Mobile: `< 640px` — ค่าเริ่มต้นและหน้าจอหลักที่ต้องออกแบบก่อน
- Tablet: `640–1023px`
- Desktop: `>= 1024px`
- ความกว้าง content สูงสุด: `max-w-7xl`

### Spacing

ใช้ระบบระยะ 4px เป็นฐาน:

- 4px: ระยะจิ๋วระหว่าง icon กับรายละเอียดรอง
- 8px: ช่องว่างภายในกลุ่ม control
- 12px: ช่องว่างระหว่าง card ขนาดเล็ก
- 16px: padding มาตรฐานของ card และ section
- 20–24px: ระยะของ hero หรือ section สำคัญ
- 32px ขึ้นไป: ใช้แยก section ใหญ่เท่านั้น

ห้ามเพิ่มค่า spacing แบบสุ่มถ้ามีค่าข้างต้นที่ใกล้เคียง

### Shape and elevation

- Input / button: `rounded-lg` ถึง `rounded-xl`
- Card / panel: `rounded-xl`
- Hero: `rounded-2xl`
- ใช้ border บางเป็นหลัก และใช้ `shadow-soft` เฉพาะ panel สำคัญหรือลอยเหนือเนื้อหา
- หลีกเลี่ยงเงาหนัก, gradient ฉูดฉาด, glass effect หลายชั้น และ radius หลายขนาดในบริเวณเดียวกัน

## 5. Color tokens

ใช้ token จาก `frontend/tailwind.config.js` ก่อนสร้างสีใหม่

| หน้าที่ | Light | Dark | การใช้งาน |
|---|---|---|---|
| App background | `paper-50` | `paper-900` | พื้นหลังทั้งหน้า |
| Soft surface | `paper-100` | `stone-800` | แถวรอง, filter, empty state |
| Main surface | `white` | `stone-900` | card, modal, cart |
| Primary | `court-500` | `court-500` | CTA และ selected state |
| Primary text | `court-700` | `court-300` | ราคา, label ที่เน้น |
| Accent | `shuttle-400/500` | `shuttle-400/500` | highlight รองที่ไม่ใช่ CTA |
| Normal text | `stone-950` | `white` | heading และข้อมูลหลัก |
| Muted text | `stone-500` | `stone-400` | metadata และ helper text |
| Border | `stone-200` | `stone-700` | เส้นแบ่งและ control |
| Destructive | `rose-600` | `rose-300` | ยกเลิกบิล, ลบ, หมดสต็อก |

ข้อกำหนด:

- สีแดงใช้กับ error/destructive เท่านั้น
- สีเขียวไม่ได้แปลว่า “สำเร็จ” อย่างเดียว แต่เป็น brand/primary action; ข้อความต้องช่วยแยกความหมาย
- ห้ามสื่อสถานะด้วยสีเพียงอย่างเดียว ต้องมีข้อความหรือไอคอนร่วมด้วย
- ทุกข้อความและ control ต้องมี contrast ที่อ่านได้ทั้ง Light/Dark mode

## 6. Typography

Font stack: `Noto Sans Thai`, `Inter`, system sans-serif

- Page title: 30px / `text-3xl`, `font-black`
- Section title: 20–24px / `text-xl–2xl`, `font-black`
- Card title และยอดเงินสำคัญ: 16–24px, `font-black`
- Body / control: 14–16px, `font-semibold` หรือ `font-bold` เมื่อเป็น action
- Metadata: 12px / `text-xs`, สี muted
- ห้ามใช้ข้อความเล็กกว่า 11px สำหรับข้อมูลที่ผู้ใช้ต้องอ่านหรือกด
- ตัวเลขราคาใช้รูปแบบ `฿1,250` และ locale `th-TH` อย่างสม่ำเสมอ
- SKU, เลขบิล และเวลาควรคัดลอกหรืออ่านแยกจากข้อความรอบข้างได้ง่าย

ข้อความ UI ต้องสั้นและเป็นคำกริยาที่บอกผลลัพธ์ เช่น “รับชำระ”, “พักยอด”, “บันทึกรายการรวม” หลีกเลี่ยง “ตกลง” ใน action สำคัญ

## 7. Components

### Buttons

- ความสูงมาตรฐาน 44px (`h-11`); CTA สำคัญหรือ modal footer ใช้ 48px (`h-12`)
- Touch target ต้องไม่น้อยกว่า 44×44px
- Primary: พื้น `court-500`, ข้อความขาว, ตัวหนา
- Secondary: border + พื้นโปร่งหรือพื้น surface
- Destructive: สี rose และต้องมีข้อความยืนยันก่อน action ที่ย้อนกลับไม่ได้
- Disabled ต้องลด opacity และไม่ตอบสนองต่อการแตะ
- Loading ต้องป้องกันการกดซ้ำ และคงความกว้างปุ่มเพื่อลด layout shift

### Inputs

- Label อยู่เหนือ input; placeholder ไม่ใช้แทน label
- สูงอย่างน้อย 44px และมี focus ring ที่เห็นชัด
- Number input ต้องระบุหน่วยใกล้ค่า เช่น บาท, ชิ้น, %
- Error แสดงชิด field ที่ผิดเมื่อระบุตำแหน่งได้ ไม่ใช้ toast อย่างเดียว
- Search ใช้ icon ค้นหาและมีปุ่มล้างเมื่อมีข้อความ

### Cards

- หนึ่ง card มีวัตถุประสงค์หลักเดียว
- hierarchy ภายใน: ชื่อ → ค่าหลัก → metadata → action
- Product card ต้องแสดงชื่อ ราคา สต็อก และสถานะขายได้/หมด
- สินค้าหมดต้อง disabled ชัดเจน แต่ยังอ่านข้อมูลสินค้าได้

### Tables and lists

- Mobile ให้ใช้ card/list ก่อน table
- ถ้าจำเป็นต้องใช้ table ต้องครอบด้วย horizontal scroll และกำหนด `min-width`
- header ของตารางต้องต่างจาก row แต่ไม่ใช้สีเข้มเกินจำเป็น
- ยอดเงินชิดขวา; ชื่อและคำอธิบายชิดซ้าย
- Empty state ต้องบอกสถานะและ next action ถ้ามี

### Modals

- Mobile เปิดแบบ bottom sheet; tablet/desktop วางกลางจอ
- สูงไม่เกิน viewport และเนื้อหาภายในเลื่อนได้
- Header และ footer ของ flow ยาวควรคงอยู่ให้เห็น
- มีชื่อ modal, ปุ่มปิดที่มี accessible label และปิดด้วย backdrop ได้เฉพาะเมื่อข้อมูลยังไม่เสี่ยงสูญหาย
- เมื่อมีข้อมูลที่แก้แล้วแต่ยังไม่บันทึก ต้องยืนยันก่อนปิด
- CTA หลักอยู่ขวาบน desktop และอยู่ล่าง/เต็มความกว้างบน mobile

### Feedback

- Success: ข้อความสั้น ระบุสิ่งที่เกิดขึ้นและยอดเมื่อเกี่ยวข้อง
- Error: อธิบายว่าทำอะไรไม่สำเร็จ พร้อมทางแก้หรือให้ลองใหม่
- Warning: ใช้ก่อนสต็อกติดลบ, ยกเลิกบิล หรือยอดชำระไม่ตรง
- ห้ามให้ feedback หายเร็วจนอ่านไม่ทัน

## 8. Screen specifications

### 8.1 Dashboard

เป้าหมาย: ตอบให้ได้ภายในไม่กี่วินาทีว่า “วันนี้ขายเท่าไร มีอะไรต้องจัดการต่อ”

- Hero แสดง `LiveMatch POS`, ชื่อหน้า, คำอธิบายสั้น และ CTA “เริ่มการขาย”
- KPI หลัก: ยอดขายวันนี้, จำนวนบิล, บิลค้าง, สินค้าใกล้หมด
- ส่วนล่างแยกเป็น “บิลค้างล่าสุด” และ “สินค้าใกล้หมด”
- Card ที่กดได้ต้องพาไปแท็บที่เกี่ยวข้องและมี affordance ชัดเจน
- อย่าเพิ่ม chart หากข้อมูลยังไม่ช่วยตัดสินใจมากกว่า KPI

### 8.2 การขาย

เป้าหมาย: เลือกสินค้า ตรวจยอด ระบุผู้ซื้อ และรับชำระโดยผิดพลาดยาก

Desktop:

- ซ้ายเป็น catalog; ขวาเป็น cart กว้างประมาณ 380px และ sticky
- Catalog ใช้ grid 3–4 คอลัมน์ตามพื้นที่

Mobile:

- Catalog ใช้ 2 คอลัมน์
- Cart ต้องเข้าถึงง่ายโดยไม่ทำให้สินค้าถูกบีบจนอ่านไม่ได้
- ถ้าเปลี่ยนเป็น cart drawer ในอนาคต ต้องมี sticky summary แสดงจำนวนชิ้นและยอดรวม

ลำดับใน cart:

1. รายการสินค้าและจำนวน
2. ผู้ซื้อ (ไม่ระบุชื่อ / สมาชิก / ผู้เล่นใน session)
3. ค่าใช้จ่ายจาก LiveMatch ที่นำมารวม ถ้ามี
4. วิธีชำระเงิน
5. ยอดย่อย, ภาษี และยอดรวม
6. “พักยอด” และ “รับชำระ”

ข้อกำหนดสำคัญ:

- “พักยอด” ใช้ได้เมื่อระบุผู้ซื้อเท่านั้น
- “รับชำระ” เป็น primary CTA และต้องแสดงยอดสุดท้ายใกล้ปุ่ม
- การเพิ่ม/ลดจำนวนต้องไม่เกินสต็อกและต้องเห็นผลทันที
- PromptPay QR ต้องแสดงชื่อผู้รับและยอดเงินกำกับ
- เมื่อยอดจากสินค้าและค่าใช้จ่ายสนามถูกรวม ต้องแยก subtotal ให้ตรวจสอบได้
- หลังชำระสำเร็จ ให้ล้าง cart และ buyer หลังแสดงผลสำเร็จชัดเจน

### 8.3 บิล

- แยก “บิลค้างชำระ” ออกจาก “ประวัติล่าสุด” ชัดเจน
- บิลค้างแสดงผู้ซื้อ, เลขบิล, เวลา, รายการย่อ และยอด
- Action “เก็บเงินรวม” เป็น primary; “ยกเลิกบิล” เป็น destructive
- การยกเลิกต้องยืนยัน พร้อมบอกว่าจะคืนสินค้าเข้าสต็อก
- สถานะใช้คำไทยที่คงที่: `ค้างชำระ`, `ชำระแล้ว`, `ยกเลิก`
- ประวัติบน mobile ใช้ list/card หรือ table ที่เลื่อนได้โดยไม่บีบข้อความ

### 8.4 สินค้า

- Desktop แสดงหมวดหมู่ด้านซ้ายและรายการสินค้าด้านขวา
- Mobile วางหมวดหมู่เป็น horizontal chips หรือ select เหนือรายการสินค้า
- Action หลักคือ “เพิ่มสินค้า”; “เพิ่มหน่วย” เป็น secondary
- Product form แบ่งข้อมูลเป็น: รูปและข้อมูลทั่วไป / ราคา / สต็อกและสถานะ
- ห้ามลบหมวดหรือหน่วยที่มีสินค้าใช้งานอยู่ ให้ปิดใช้งานแทน
- รูปสินค้าใช้สัดส่วนคงที่และ `object-cover`; preview ในฟอร์มใช้ `object-contain`

### 8.5 สต็อก

- Hero ใช้พื้นเข้มเหมือน Dashboard เพื่อแยก operational area
- Action หลักสามแบบต้องแยกความหมาย: `นำเข้า`, `นำออก`, `ปรับปรุงสต็อก`
- แสดง summary อย่างน้อย: จำนวนสินค้า, จำนวนหน่วยรวม, มูลค่าสต็อก, สินค้าใกล้หมด
- Filter ที่ต้องมี: ทั้งหมด, ใกล้หมด, หมดสต็อก
- Batch modal ต้องแสดงคงเหลือเดิม, การเปลี่ยนแปลง, ยอดหลังรายการ และต้นทุน
- ค่า “ยอดหลังรายการ” ต้องเด่นกว่าค่าเดิม
- ห้ามบันทึกยอดติดลบ และต้องเตือนก่อนผลลัพธ์ผิดปกติ

### 8.6 รายงาน

- เริ่มจาก KPI ที่ใช้ตัดสินใจ: ยอดขาย, ต้นทุน, กำไรขั้นต้น, ยอดค้าง, สินค้าใกล้หมด
- ระบุช่วงเวลาของข้อมูลทุกครั้ง เช่น “วันนี้” หรือวันที่เริ่ม–จบ
- กำไรต้องแยกจากยอดขายด้วย label ไม่ใช้สีอย่างเดียว
- ถ้าเพิ่มกราฟ ต้องมีค่าตัวเลขหรือ table ที่อ่านได้โดยไม่พึ่งกราฟ

### 8.7 ตั้งค่า

- จัดกลุ่มเป็น: การแสดงผล, ภาษี, PromptPay, ใบเสร็จ, สต็อก
- ค่าที่กระทบเงิน เช่น ภาษีและราคารวมภาษี ต้องมี helper text อธิบายผล
- แสดง preview ของโลโก้และใบเสร็จเมื่อทำได้
- ปุ่มบันทึกต้องอยู่ท้ายฟอร์มและเข้าถึงได้ง่ายบน mobile
- เปลี่ยน theme แล้วต้องเห็นผลทันที แต่ยังต้องบันทึกตาม behavior ของระบบ

## 9. Responsive behavior

- ออกแบบและตรวจที่ viewport อย่างน้อย `390×844`, `768×1024`, `1440×1024`
- ห้ามมี horizontal overflow ระดับหน้า
- Grid ลดจำนวนคอลัมน์ตามพื้นที่โดยไม่ทำให้ card แคบจนข้อมูลตัดผิดความหมาย
- ปุ่ม action สามารถ wrap ได้ แต่ CTA หลักต้องไม่หลุดจากสายตา
- Modal บน mobile ต้องไม่ถูก keyboard บัง field หรือ CTA
- Bottom navigation และ modal footer ต้องรองรับ `env(safe-area-inset-bottom)`

## 10. Accessibility

- ใช้ semantic element: `button`, `nav`, `main`, `section`, `table`, `label`
- Icon-only button ทุกปุ่มต้องมี `aria-label`
- Focus state ต้องมองเห็นชัดและลำดับ tab ต้องเป็นไปตามหน้าจอ
- Modal ต้องจัดการ focus, ปิดด้วย `Escape`, และคืน focus ไปยังปุ่มเดิม
- Touch target ขั้นต่ำ 44×44px
- ข้อความ body ควรมี contrast อย่างน้อย 4.5:1
- Animation ต้องเคารพ `prefers-reduced-motion`
- รูปสินค้าต้องมี alt เป็นชื่อสินค้า; รูปตกแต่งใช้ alt ว่าง

## 11. Content and localization

- เขียนข้อความไทยให้สั้น ตรง และใช้ศัพท์เดิมทั้งระบบ
- ห้ามต่อประโยคด้วย string หลายส่วนจนแปลภาษาอื่นไม่ได้
- วันที่ เวลา จำนวน และเงินใช้ formatter ตาม locale
- Layout ต้องเผื่อข้อความ English ยาวกว่าภาษาไทยประมาณ 30%
- คำมาตรฐาน: สินค้า, หมวดหมู่, หน่วย, สต็อก, บิล, พักยอด, รับชำระ, ยกเลิกบิล
- Error จาก backend ควรถูก map เป็นข้อความที่ผู้ใช้เข้าใจ ไม่แสดงข้อความเทคนิคดิบ

## 12. State coverage

ทุกหน้าหรือ component ใหม่ต้องออกแบบ state ต่อไปนี้ให้ครบ:

- Loading: ใช้ skeleton หรือ progress ที่ไม่ทำให้ layout กระโดด
- Empty: อธิบายว่ายังไม่มีอะไรและเริ่มต้นอย่างไร
- Error: บอกปัญหาและมี retry เมื่อเหมาะสม
- Disabled: เห็นว่าใช้ไม่ได้และอธิบายเหตุผลเมื่อไม่ชัด
- Success: ยืนยันผลโดยไม่บังงานถัดไป
- Long content: ชื่อสินค้า, ชื่อลูกค้า, SKU และเลขบิลยาว
- Offline/slow network: ป้องกัน submit ซ้ำและไม่แสดงว่าสำเร็จก่อน server ยืนยัน
- Read-only/feature disabled: ข้อมูลยังอ่านได้ แต่ action ที่แก้ข้อมูลต้องถูกซ่อนหรือ disabled อย่างสม่ำเสมอ

## 13. Do / Don't

Do:

- ใช้ token และ component pattern เดิมก่อนสร้างแบบใหม่
- ทำยอดรวมและ CTA หลักให้มองเห็นง่าย
- แสดงผลของ action ที่กระทบเงินหรือสต็อกอย่างชัดเจน
- ใช้ progressive disclosure กับข้อมูลขั้นสูง
- ทดสอบด้วยข้อมูลจริงที่ชื่อยาว รายการเยอะ และยอดเป็นศูนย์

Don't:

- อย่าใช้สีใหม่เพื่อแก้ปัญหา hierarchy
- อย่าซ่อน action หลักไว้ในเมนูสามจุด
- อย่าให้ผู้ใช้เดาความหมายของ icon หรือสถานะจากสี
- อย่าใช้ modal ซ้อน modal ถ้าหลีกเลี่ยงได้
- อย่าล้าง cart หรือ form เมื่อ API ล้มเหลว
- อย่าแสดงทศนิยมของเงินบาทถ้าระบบยังเก็บเป็นจำนวนเต็มบาท
- อย่าเพิ่ม dashboard metric ที่ไม่มีคำถามทางธุรกิจรองรับ

## 14. Definition of done

ก่อน merge งาน POS ต้องตรวจครบ:

- [ ] ใช้สี, spacing, radius และ typography ตามเอกสารนี้
- [ ] ใช้งาน flow หลักได้ที่ 390px โดยไม่มี horizontal overflow
- [ ] Light mode และ Dark mode อ่านได้ครบ
- [ ] Loading, empty, error, disabled และ success state ครบ
- [ ] Keyboard focus และ screen-reader label ของ icon button ครบ
- [ ] Action ที่กระทบเงินหรือสต็อกมี confirmation/feedback เหมาะสม
- [ ] ยอดรวม ภาษี และสถานะบิลตรงกับข้อมูลจาก backend
- [ ] ข้อความไทยถูกต้อง และ layout พร้อมรองรับ English
- [ ] Vitest ที่เกี่ยวข้องผ่าน
- [ ] ตรวจภาพจริงอย่างน้อย 390×844 และ 1440×1024
- [ ] ไม่มี console error หรือ warning ใหม่

## 15. Current implementation references

- POS screen: `frontend/src/pages/POSPage.vue`
- POS tests: `frontend/src/pages/POSPage.test.js`
- Theme tokens: `frontend/tailwind.config.js`
- Global styles: `frontend/src/style.css`
- Theme behavior: `frontend/src/theme.js`
- Localization: `frontend/src/i18n.js`
- POS API: `backend/pos.go`

ถ้า implementation เดิมขัดกับเอกสารนี้ ให้พิจารณาผลกระทบต่อ flow และข้อมูลก่อนแก้ เอกสารนี้ควรถูกอัปเดตพร้อม code เมื่อมีการเปลี่ยน design direction ที่ตั้งใจไว้
