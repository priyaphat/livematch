# Known Issues

บันทึกเฉพาะปัญหาที่ยอมรับชั่วคราว ห้ามใช้ไฟล์นี้ซ่อน P0/P1

| Issue | Case ID | Severity | Impact | Workaround | Owner | Due | Retest |
|---|---|---|---|---|---|---|---|
| ค้นสมาชิกที่เก็บเบอร์แบบ `+66` ด้วยเลขไทย 10 หลักไม่พบ | POS-XMEM-005 | P2 | Hold Order ที่พิมพ์ `08xxxxxxxx` อาจไม่พบสมาชิกซึ่งสร้างสำเร็จแล้ว | ค้นด้วยชื่อสมาชิก หรือค้นด้วย `+66` | Backend | TBD | รัน API search ทั้ง 3 รูปแบบ: ชื่อ, 08, +66 |
| สีประเภทในประวัติเคลื่อนไหวสต็อกไม่ตรงข้อกำหนด: รับเข้าแดง, จ่ายออกเหลือง, ปรับยอดเทา | POS-STOCK-006 | P2 | ผู้ใช้แยกประเภทรายการด้วยสีผิดและเสี่ยงอ่านประวัติคลาดเคลื่อน | อ่านข้อความประเภทแทนสี | POS Frontend | TBD | Playwright ตรวจ class สี เขียว/แดง/ส้ม |
| เอกสารปรับยอดที่ลดจำนวนถูก API แสดงประเภทเป็น `out` แทน `adjust` เพราะ mapper ตีความทุก delta ติดลบเป็นจ่ายออก | POS-STOCK-012 | P2 | ประวัติและสีประเภททำให้เข้าใจว่ามีเอกสารจ่ายออก ทั้งที่เป็นผลจากการตรวจนับปรับยอด | ตรวจ `reason=adjustment` และเลขเอกสารประกอบ | Backend | TBD | รันปรับยอด 2→1 แล้วตรวจ type ต้องเป็น `adjust` |
| คอลัมน์เหตุผลประวัติสต็อกแสดง technical code เช่น `sale`, `void`, `restock`, `adjustment` | POS-STOCK-006 | P3 | หน้าประวัติอ่านยากและไม่เป็นภาษาเดียวกับ UI | อ้างอิงคอลัมน์ประเภทและเลขเอกสาร | POS Frontend | TBD | เปิดแท็บประวัติเคลื่อนไหวหลังทำครบทุกประเภท |
| Dashboard ปัดยอดเฉลี่ยต่อบิลลงแทนการปัดครึ่งขึ้น และ UI ตัดสตางค์ทิ้ง (API 98.31 แทน 98.32, UI แสดง 98) | POS-DASH-001, POS-DASH-003 | P2 | ตัวเลขสรุป Dashboard ไม่ตรงผลหารยอดขายจริง | อ้างอิงยอดขายรวมและจำนวนบิล หรือคำนวณแยกชั่วคราว | Backend / POS Frontend | TBD | Playwright เปรียบเทียบ API และข้อความ UI กับผลรวม/จำนวนบิล |
| Dashboard แสดงข้อความ `5 หมวดหมู่หลัก` แบบ hardcode แม้จำนวนหมวดหมู่จริงไม่เท่ากับ 5 | POS-DASH-003 | P3 | หัวข้อสรุปทำให้เข้าใจจำนวนหมวดหมู่ผิด | นับรายการหมวดหมู่ที่แสดงจริง | POS Frontend | TBD | Playwright เปรียบเทียบข้อความกับ `categories.length` |
| การ์ดพักยอด/สต็อกต่ำและแถวบิลล่าสุดเป็น `div onClick` ไม่มี button/link semantics หรือ keyboard focus | POS-DASH-006 | P3 | ผู้ใช้คีย์บอร์ดและเทคโนโลยีช่วยเหลือเปิดทางลัดไม่ได้ | ใช้ปุ่มข้อความ “จัดการสต็อกทั้งหมด” และ “ดูประวัติบิลทั้งหมด” | POS Frontend | TBD | Playwright ตรวจ accessible role และ Tab/Enter |
